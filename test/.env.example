# Example .env for Test scripts
DEVICE_HOST=192.168.100.22
DEVICE_USER=admin
DEVICE_PASS=Admin123
DEVICE_ID=1

# Local server where monitor endpoints are exposed (the repo server)
SERVER_HOST=192.168.0.125
SERVER_PORT=8000
SERVER_PATH=/api/notifications

# Polling settings
POLL_TIMEOUT=120
POLL_INTERVAL=3
